import React, {useState} from 'react';
import { Text, StyleSheet, View, Image, TextInput } from "react-native";

const ImageScreenjs = ({e}) => {

        
   return (
       <View> 
            <Text>{e}</Text>
       </View>
   );
};
      

const styles = StyleSheet.create({

})

export default ImageScreenjs